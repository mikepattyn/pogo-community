# ===========================================
# POGO Community - Docker Environment Variables
# ===========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control

# ===========================================
# MySQL Database (API)
# ===========================================
MYSQL_ROOT_PASSWORD=your_secure_root_password_here
MYSQL_USER=pogo_api_user
MYSQL_PASSWORD=your_secure_mysql_password_here

# ===========================================
# MSSQL Database (Bot)
# ===========================================
# Password must meet SQL Server requirements:
# - At least 8 characters
# - Contains uppercase, lowercase, numbers, and special characters
MSSQL_SA_PASSWORD=YourSecure!Password123

# ===========================================
# API Backend Configuration
# ===========================================
JWT_KEY=your_jwt_secret_key_minimum_32_characters_long

# ===========================================
# Discord Bot Configuration
# ===========================================
# Get your bot token from: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# ===========================================
# Google Cloud Platform Configuration
# ===========================================
# Your Google Cloud Project ID
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id

# Path to your Google Cloud service account credentials JSON file
# Leave empty if using default credentials or workload identity
GOOGLE_APPLICATION_CREDENTIALS=

# ===========================================
# Usage Instructions
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in all the values above with your actual credentials
# 3. Ensure .env is in your .gitignore (it already is)
# 4. Run: docker-compose up -d
#
# Port Mappings:
# - API: http://localhost:1000
# - Bot: Runs in background (Discord integration)
# - Web App: http://localhost:3000
#
# Database Access (from host):
# - MySQL: Not exposed (internal only)
# - MSSQL: Not exposed (internal only)
#
# For development access to databases, you can temporarily expose ports
# by adding to docker-compose.yml:
#   mysql:
#     ports:
#       - "3306:3306"
#   mssql:
#     ports:
#       - "1433:1433"
