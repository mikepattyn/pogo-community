meta {
  name: Join Raid
  type: http
}

post {
  url: {{raid_service_url}}/api/raid/by-message/{{messageId}}/join
  body: json
}

body:json {
  {
    "playerId": 1
  }
}

docs {
  # Join Raid

  Adds a player to a raid's participant list.

  ## Path Parameters
  - `messageId`: Discord message ID of the raid

  ## Request Body
  - `playerId`: Player ID joining the raid

  ## Response
  Returns success if player was added.
}

