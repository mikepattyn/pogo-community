meta {
  name: Get Active Raids
  type: http
}

get {
  url: {{raid_service_url}}/api/raid/active?pageNumber=1&pageSize=50
}

docs {
  # Get Active Raids

  Retrieves currently active raids with pagination.

  ## Query Parameters
  - `pageNumber`: Page number for pagination (default: 1)
  - `pageSize`: Number of results per page (default: 50)

  ## Response
  Returns collection of active raids.
}

