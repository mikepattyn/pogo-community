meta {
  name: Get All Gyms
  type: http
}

get {
  url: {{gym_service_url}}/api/gym?activeOnly=true&pageNumber=1&pageSize=50
}

docs {
  # Get All Gyms

  Retrieves all gyms with pagination support.

  ## Query Parameters
  - `activeOnly`: Return only active gyms (default: true)
  - `pageNumber`: Page number for pagination (default: 1)
  - `pageSize`: Number of results per page (default: 50)

  ## Response
  Returns a collection of gyms with pagination metadata.
}

