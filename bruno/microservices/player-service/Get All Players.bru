meta {
  name: Get All Players
  type: http
}

get {
  url: {{player_service_url}}/api/player?activeOnly=true&pageNumber=1&pageSize=50
}

docs {
  # Get All Players

  Retrieves all players with pagination support.

  ## Query Parameters
  - `activeOnly`: Return only active players (default: true)
  - `pageNumber`: Page number for pagination (default: 1)
  - `pageSize`: Number of results per page (default: 50)

  ## Response
  Returns a collection of players with pagination metadata.
}

