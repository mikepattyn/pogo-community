apiVersion: v1
kind: Secret
metadata:
  name: db-secrets
  namespace: pogo-system
type: Opaque
stringData:
  # CockroachDB credentials
  DB_USERNAME: root
  DB_PASSWORD: ""  # CockroachDB default root user has no password in insecure mode
  MSSQL_SA_PASSWORD: "YourStrong@Passw0rd"  # Keep for reference during migration

