apiVersion: v1
kind: Service
metadata:
  name: ocr-service
  namespace: pogo-system
  labels:
    app: ocr-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5001
      targetPort: 5001
      protocol: TCP
  selector:
    app: ocr-service
